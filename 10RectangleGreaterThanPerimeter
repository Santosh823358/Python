length = int(input("enter the length of rectangle:"))
breadth = int(input("enter the breadth of rectangle:"))
area = length * breadth
perimeter = 2*(length + breadth)
if(area > perimeter):
    print("Area of rectangle is greater than prrimeter")
else:
    print("smaller")